/** @typedef {import('./state.js').Presentation} Presentation */

/** @typedef {{ id: string, name: string, kind: string, bytes: number }} DocumentSummary */
/** @typedef {{ phase: 'idle'|'uploading'|'generating'|'validating'|'rendering'|'error'|'done', percent: number, logs: string[], error: string|null }} GenerationState */
/** @typedef {{ presentation: Presentation|null, selectedSlide: number, dirty: boolean, quality: object|null }} PresentationState */
/** @typedef {{ outputPath: string, format: string, artifactId: string, downloadUrl: string }} ArtifactResult */
/** @typedef {{ value: object|null, sourceName: string|null, warnings: string[] }} ThemeState */
/** @typedef {{ documents: DocumentSummary[], presentation: Presentation|null, selectedSlide: number, dirty: boolean, quality: object|null, theme: object|null, themeSource: string|null, themeWarnings: string[], busy: boolean, generation: GenerationState, models: string[], settings: { host: string, model: string, slides: number, query: string, embeddingModel: string, format: 'pptx'|'html'|'pdf'|'json', output: string, strictQuality: boolean } }} AppState */

export const LAYOUTS = ['title', 'agenda', 'content', 'comparison', 'process', 'summary', 'table', 'timeline', 'key-message'];

export const DEFAULT_THEME = {
  fontFace: 'Aptos',
  headingFontFace: 'Aptos Display',
  colors: { ink: '111827', muted: '6B7280', line: 'E5E7EB', accent: '2563EB', canvas: 'FFFFFF', surface: 'F9FAFB' },
  footer: 'Generated by Local Deck AI',
  margin: 0.85
};
